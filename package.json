{
  "name": "mikaty-website",
  "version": "1.0.0",
  "description": "Mikaty website with React app",
  "scripts": {
    "build": "cd mikaty-vitrine && npm install && npm run build",
    "start": "cd mikaty-vitrine && npm start",
    "dev": "cd mikaty-vitrine && npm start"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
